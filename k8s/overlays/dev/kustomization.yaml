apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ohisee-dev

resources:
  - ../../base

patches:
  - path: patches/namespace.yaml
  - path: patches/replicas.yaml
  - path: patches/configmap.yaml
  - path: patches/images.yaml
  - path: patches/ingress.yaml
