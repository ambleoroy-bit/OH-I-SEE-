(() => {
const toolbar=document.querySelector('.blueprint-viewer-toolbar'),button=document.createElement('button');button.className='bp-download-btn';button.type='button';button.textContent='↓ CAD JSON';toolbar.append(button);
const panel=document.createElement('details');panel.className='bp-cad-panel';panel.innerHTML='<summary>Dimensions & schedules · structured drawing data</summary><div class="bp-cad-content"></div>';document.getElementById('bp-canvas-wrap').after(panel);
const esc=s=>String(s??'To confirm').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
window.currentBlueprintCAD=()=>BlueprintCAD.create(requirements,planRooms(requirements,requirements.plotLength*8,requirements.plotWidth*8,5));
button.onclick=()=>{const data=currentBlueprintCAD(),url=URL.createObjectURL(new Blob([JSON.stringify(data,null,2)],{type:'application/json'})),a=document.createElement('a');a.href=url;a.download='ohisee-floor-plan.json';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);};
panel.ontoggle=()=>{if(!panel.open)return;const d=currentBlueprintCAD();const table=(headers,rows)=>'<div class="bp-cad-table"><table><thead><tr>'+headers.map(h=>'<th>'+esc(h)+'</th>').join('')+'</tr></thead><tbody>'+rows.map(row=>'<tr>'+row.map(v=>'<td>'+esc(v)+'</td>').join('')+'</tr>').join('')+'</tbody></table></div>';panel.querySelector('div').innerHTML='<p>Ground-floor concept · Dimensions in feet · Areas in sq.ft</p>'+table(['Room','Length','Width','Area'],d.room_schedule.map(a=>[a.room_name,a.length,a.width,a.area_sqft]))+'<h3>Door schedule</h3>'+table(['Door','Type','Width','Height'],d.door_schedule.map(a=>[a.door_id,a.door_type,a.width,a.height]))+'<h3>Window schedule</h3>'+table(['Window','Type','Width','Height'],d.window_schedule.map(a=>[a.window_id,a.window_type,a.width,a.height]))+'<p>Conceptual carpet area: '+d.carpet_area+' sq.ft · Parking: '+d.parking_area+' sq.ft</p><p>Built-up/plinth area, wall thickness and setbacks remain unconfirmed. Upper floors are not represented by this ground-floor drawing.</p>';};
})();
